// Player Class
class Player
{
    int Health;
    Vector2 Position;
    List<Bullet> Bullets;

    void Update(GameTime gameTime)
    {
        // Handle player movement
        // Handle player shooting
    }

    void TakeDamage()
    {
        Health--;
        if (Health <= 0)
        {
            // Handle player death
        }
    }

    void Reset()
    {
        Health = 10;
        Position = StartingPosition;
        Bullets.Clear();
    }

    bool BulletsCollideWith(GameEntity entity)
    {
        // Check collision between player bullets and entity
    }
}